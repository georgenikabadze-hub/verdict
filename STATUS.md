NEXT: Human submission tasks by George  (last updated 2026-04-26T00:06:27Z)

Submission-prep batch is applied: Tavily tariff lookup is wired into `/api/quote` and `/quote`, the quote Solar badge now distinguishes measured vs estimated segments, and README is rewritten for jury review. `pnpm tsc --noEmit` and `pnpm test` pass; full `pnpm lint` still fails on pre-existing generated Cesium files under `public/cesium`, while linting the touched source files passes.
